# Configuration Railway - Variables d'Environnement
# Copiez ces variables dans Railway UI → Variables

# Base de données (automatique avec Railway PostgreSQL)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Port (automatique sur Railway)
PORT=${{PORT}}

# JWT Configuration - IMPORTANT: Générez un secret fort!
# Générer avec: openssl rand -base64 32
# Ou avec: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=REMPLACER_PAR_SECRET_GENERE_32_CARACTERES_MINIMUM
JWT_EXPIRES_IN=7d

# Email Configuration (Gmail exemple)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=votre_email@gmail.com
EMAIL_PASSWORD=mot_de_passe_application_google
EMAIL_FROM=noreply@gestion-apparts.com

# PawaPay Configuration
PAWAPAY_API_KEY=votre_cle_api_pawapay
PAWAPAY_API_URL=https://api.pawapay.cloud

# Frontend URL (mettre à jour après déploiement Vercel)
FRONTEND_URL=https://votre-app.vercel.app

# Nom du projet Vercel (pour la sécurité CORS)
# Utilisez le nom exact de votre projet Vercel (visible dans l'URL: nom-projet.vercel.app)
VERCEL_PROJECT_NAME=gestion-apparts

# Environment
NODE_ENV=production

# Limite de téléchargement
MAX_FILE_SIZE=100mb
