# Docker Compose Environment Variables
# Copy this file to .env in the root directory and update the values

# API Configuration
JWT_SECRET=your-secret-key-change-in-production-generate-a-long-random-string
FRONTEND_URL=http://localhost:3000

# Web Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000/api
PAWAPAY_API_KEY=your-pawapay-api-key

# Note: These variables will be used by docker-compose.yml
# The database connection is already configured in docker-compose.yml
